# Nagent Blueprint — generated by Architecture Agent
# Keep this file updated so generated apps stay aligned with the Nagent AI platform.

version: "1"
platform: nagent
generated_by: architecture_agent

buildcraft:
  watermark: "Generated using Buildcraft by Nagent"

stack:
  frontend: "react"
  backend: "express"
  database: "postgresql"

layout:
  app_root: "app"
  migrations_dir: "db/migrations"
  seeds_dir: "db/seeds"

db_scripts:
  notes: |
    Migrations applied in lexical order (001_, 002_, ...). Seeds applied after migrations via psql $DATABASE_URL < db/seeds/001_seed.sql. Only the seed file is updated in this pass; no schema changes.

artifacts:
  architecture: docs/Architecture.md
  db_schema_json: db-schema.json